{
  "id": "webhook",
  "name": "WebHook",
  "description": "Send alerts to a custom webhook.",
  "version": "1.9.4",
  "stream": "yes",
  "settings": {
    "USE_WEBHOOK": {
      "context": "multisite",
      "default": "no",
      "help": "Enable sending alerts to a custom webhook.",
      "id": "use-webhook",
      "label": "Use webhook",
      "regex": "^(yes|no)$",
      "type": "check"
    },
    "WEBHOOK_URL": {
      "context": "global",
      "default": "https://api.example.com/bw",
      "help": "Address of the webhook.",
      "id": "webhook-url",
      "label": "Webhook URL",
      "regex": "^.*$",
      "type": "text"
    },
    "WEBHOOK_RETRY_IF_LIMITED": {
      "context": "global",
      "default": "no",
      "help": "Retry to send the request if the remote server is rate limiting us (may consume a lot of resources).",
      "id": "webhook-retry-if-limited",
      "label": "Retry if limited by webhook",
      "regex": "^(yes|no)$",
      "type": "check"
    },
    "WEBHOOK_ALERT_IPS": {
      "context": "global",
      "default": "",
      "help": "Only notify denied requests from these IPs/networks (separated with spaces). Leave empty to notify every denied request.",
      "id": "webhook-alert-ips",
      "label": "Alert IPs",
      "regex": "^[0-9a-fA-F:./ ]*$",
      "type": "text"
    },
    "WEBHOOK_UNLISTED_THRESHOLD": {
      "context": "global",
      "default": "0",
      "help": "When WEBHOOK_ALERT_IPS is set, send one summary notification if this many requests from other IPs are denied within WEBHOOK_UNLISTED_PERIOD (0 to disable).",
      "id": "webhook-unlisted-threshold",
      "label": "Unlisted IPs threshold",
      "regex": "^[0-9]+$",
      "type": "text"
    },
    "WEBHOOK_UNLISTED_PERIOD": {
      "context": "global",
      "default": "600",
      "help": "Period in seconds used to count denied requests from IPs not in WEBHOOK_ALERT_IPS.",
      "id": "webhook-unlisted-period",
      "label": "Unlisted IPs period",
      "regex": "^[1-9][0-9]*$",
      "type": "text"
    }
  }
}
