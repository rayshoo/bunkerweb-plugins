{
  "id": "discord",
  "name": "Discord",
  "description": "Send alerts to a Discord channel (using webhooks).",
  "version": "1.9.2",
  "stream": "yes",
  "settings": {
    "USE_DISCORD": {
      "context": "multisite",
      "default": "no",
      "help": "Enable sending alerts to a Discord channel.",
      "id": "use-discord",
      "label": "Use Discord",
      "regex": "^(yes|no)$",
      "type": "check"
    },
    "DISCORD_WEBHOOK_URL": {
      "context": "global",
      "default": "https://discordapp.com/api/webhooks/...",
      "help": "Address of the Discord Webhook.",
      "id": "discord-webhook-url",
      "label": "Discord webhook URL",
      "regex": "^.*$",
      "type": "password"
    },
    "DISCORD_RETRY_IF_LIMITED": {
      "context": "global",
      "default": "no",
      "help": "Retry to send the request if Discord API is rate limiting us (may consume a lot of resources).",
      "id": "discord-retry-if-limited",
      "label": "Retry if limited by Discord",
      "regex": "^(yes|no)$",
      "type": "check"
    },
    "DISCORD_ALERT_IPS": {
      "context": "global",
      "default": "",
      "help": "Only notify denied requests from these IPs/networks (separated with spaces). Leave empty to notify every denied request.",
      "id": "discord-alert-ips",
      "label": "Alert IPs",
      "regex": "^[0-9a-fA-F:./ ]*$",
      "type": "text"
    },
    "DISCORD_UNLISTED_THRESHOLD": {
      "context": "global",
      "default": "0",
      "help": "When DISCORD_ALERT_IPS is set, send one summary notification if this many requests from other IPs are denied within DISCORD_UNLISTED_PERIOD (0 to disable).",
      "id": "discord-unlisted-threshold",
      "label": "Unlisted IPs threshold",
      "regex": "^[0-9]+$",
      "type": "text"
    },
    "DISCORD_UNLISTED_PERIOD": {
      "context": "global",
      "default": "600",
      "help": "Period in seconds used to count denied requests from IPs not in DISCORD_ALERT_IPS.",
      "id": "discord-unlisted-period",
      "label": "Unlisted IPs period",
      "regex": "^[1-9][0-9]*$",
      "type": "text"
    }
  }
}
